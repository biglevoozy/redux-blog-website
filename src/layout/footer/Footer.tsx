import styles from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <div className={styles.name}>Blog Website &copy;, 2025 </div>
    </footer>
  );
};

export default Footer;
